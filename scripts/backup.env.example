# Copy to /opt/totem-bot/shared/backup.env on the server and set mode 0600.
# The recipient is a public GPG key fingerprint; do not put a private key here.
BACKUP_DIR=/var/backups/totem-bot
BACKUP_RETENTION_DAYS=30
BACKUP_GPG_RECIPIENT=REPLACE_WITH_OWNER_GPG_FINGERPRINT
